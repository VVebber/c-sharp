import type { UserResource } from '@clerk/types';
import { Avatar } from '../elements';
import type { PropsOfComponent } from '../styledSystem';
type UserAvatarProps = PropsOfComponent<typeof Avatar> & Partial<Pick<UserResource, 'firstName' | 'lastName' | 'profileImageUrl'>> & {
    name?: string | null;
};
export declare const UserAvatar: (props: UserAvatarProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export {};
