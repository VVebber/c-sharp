import type { FieldId } from '@clerk/types';
import React from 'react';
import type { LocalizationKey } from '../customizables';
import { Input } from '../customizables';
import type { PropsOfComponent } from '../styledSystem';
type FormControlProps = Omit<PropsOfComponent<typeof Input>, 'label' | 'placeholder'> & {
    id: FieldId;
    isRequired?: boolean;
    isOptional?: boolean;
    errorText?: string;
    onActionClicked?: React.MouseEventHandler;
    isDisabled?: boolean;
    label: string | LocalizationKey;
    placeholder?: string | LocalizationKey;
    actionLabel?: string | LocalizationKey;
};
export declare const FormControl: (props: FormControlProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export {};
